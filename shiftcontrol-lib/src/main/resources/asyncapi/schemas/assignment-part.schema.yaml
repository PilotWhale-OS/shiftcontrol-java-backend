title: AssignmentPart
type: object
additionalProperties: false

required:
  - volunteerId
  - status
  - positionSlot

properties:
  volunteerId:
    type: string
  status:
    type: string
    description: AssignmentStatus enum
    enum:
      - REQUESTED
      - OFFERED
      - ACCEPTED
      - REJECTED
  positionSlot:
    $ref: './position-slot-part.schema.yaml'

x-javaType: org.shiftcontrol.lib.asyncapi.events.parts.AssignmentPart
